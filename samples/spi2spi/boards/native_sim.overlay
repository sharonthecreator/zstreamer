/*
 * Copyright (c) 2026 sharonthecreator
 * SPDX-License-Identifier: Apache-2.0
 *
 * SPI-to-SPI relay on native_sim using emulated SPI.
 */

/ {
	spi_src: spi-emul-src {
		compatible = "zephyr,spi-emul-controller";
		#address-cells = <1>;
		#size-cells = <0>;
		status = "okay";
	};

	spi_sink: spi-emul-sink {
		compatible = "zephyr,spi-emul-controller";
		#address-cells = <1>;
		#size-cells = <0>;
		status = "okay";
	};

	streaming_graph: streaming-graph {
		compatible = "zstreamer,graph";
		#address-cells = <1>;
		#size-cells = <0>;
		buffer-count = <8>;
		buffer-size = <128>;

		spi_source: spi-source@0 {
			compatible = "zstreamer,zstsrc-spi";
			reg = <0>;
			spi-device = <&spi_src>;
			spi-max-frequency = <1000000>;
			rx-length = <64>;
			children = <&spi_sinker>;
		};

		spi_sinker: spi-sinker@1 {
			compatible = "zstreamer,zstsink-spi";
			reg = <1>;
			spi-device = <&spi_sink>;
			spi-max-frequency = <1000000>;
		};
	};
};
